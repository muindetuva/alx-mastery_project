/**
 * Compatibility manifest for curriculum tooling that expects this file.
 * Tailwind CSS v4 is configured through @tailwindcss/vite and src/input.css.
 */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  darkMode: "class",
};
