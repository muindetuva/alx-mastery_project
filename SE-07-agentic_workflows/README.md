# TaskFlow

TaskFlow is a command-line task and project manager built around a `cmd` REPL,
object-oriented Python models, and JSON file persistence. It manages Projects
and Tasks, supports standard CRUD commands, and adds task-specific workflow
commands such as completing tasks, assigning tasks to projects, listing overdue
work, and summarising task counts by status and priority.
